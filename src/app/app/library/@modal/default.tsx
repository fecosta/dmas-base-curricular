/**
 * No contextual detail belongs to this URL.
 *
 * Rendered for every Library URL that is not an intercepted module route, and
 * for a module route reached directly or by reload — where the standalone page
 * is the whole experience and an overlay would be a second copy of it.
 */
export default function NoContextualDetail() {
  return null;
}
